"""9Bot Cloud Hosting — server-side components.

Submodules:
- cloud_config: Cloud-specific defaults and configuration
- vm_agent: Per-VM management service (BlueStacks + 9Bot lifecycle)
- orchestrator: Central API for managing VMs, instances, and users
- models: SQLite persistence layer
"""
